-- create member table
SELECT * FROM MEMBER select * from member;MEMBER DROP TABLE IF EXISTS member CASCADE;

CREATE TABLE member
(
    ID BIGINT GENERATED BY DEFAULT AS IDENTITY,
    NAME VARCHAR(255),
    PRIMARY KEY (ID)
);